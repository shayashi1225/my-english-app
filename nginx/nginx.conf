location / {
    try_files $uri $uri/ /index.html;
}

location /api/ {
    resolver 10.89.2.1 valid=10s;
    set $backend http://english-backend:8000;
    proxy_pass $backend$request_uri;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}
